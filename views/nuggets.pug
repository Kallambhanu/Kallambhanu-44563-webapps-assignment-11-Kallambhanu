extends layout

block content
  h1= title
  -
  - var result = results
  div.container-fluid
    div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) nugget Name: #{item["nugget_name"]}  <br>  nugget_size: #{item["nugget_size"]} <br> nugget_type: #{item["nugget_type"]} 
  script.
    function buy(index){
        alert("you purchased item "+(index+1))
        }