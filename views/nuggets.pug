extends layout

block content
  h1= title
  -
  - var result = results
  div.container-fluid
    div.row
        each item,index in result
            div.col-sm-2(onclick=`buy(${index})`) nuggets_type: #{item["nuggets_type"]}  <br>  size: #{item["size"]} <br> cost: #{item["cost"]} 
  script.
    function buy(index){
        alert("you purchased item "+(index+1))
        }